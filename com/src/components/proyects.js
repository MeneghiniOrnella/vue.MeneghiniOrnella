/* import { 
    FaHtml5, FaCss3Alt, FaJsSquare, FaPython, FaDatabase, FaPhp, FaWordpress, FaMarkdown,
    FaBootstrap, FaSass, FaReact, FaFlask,
    FaRegStickyNote, FaTrello, FaGit, FaGithub, FaCode, FaEdit, FaFileExcel, FaFilePowerpoint, FaFileWord
} from "react-icons/fa"; */

/* import {useState} from "react";
import Card from "./card.js";
import data from "../helpers/dataProyect.js";

import "../App.css"

const Proyects =()=> {
    const [proyects] = useState(data);
    return(
        <><h2>Proyectos</h2>
        <div className="skill_div">
            {proyects.map((proyect)=>{
                return(
                    <div>
                    <Card 
                        name={proyect.name}
                        github={proyect.github}
                        tech={proyect.tech}
                        descrition={proyect.description}
                    />
                    </div>
                )
            })}
        </div></>
    )
};

export default Proyects; */